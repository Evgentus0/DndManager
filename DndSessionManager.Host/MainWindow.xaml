<Window x:Class="DndSessionManager.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DndSessionManager.Host"
        mc:Ignorable="d"
        Title="D&amp;D Session Manager - Host" Height="600" Width="700"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="D&amp;D Session Manager Server"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <!-- Main Content: Two Columns -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column: Controls -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <!-- Server Status -->
                <GroupBox Header="Server Status" Margin="0,0,0,10" Padding="10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Status: " FontSize="14"/>
                            <TextBlock x:Name="StatusText"
                                       Text="Stopped"
                                       FontSize="14"
                                       FontWeight="Bold"
                                       Foreground="Red"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="Connected Users: " FontSize="14"/>
                            <TextBlock x:Name="UsersText"
                                       Text="0"
                                       FontSize="14"
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- Port Configuration -->
                <GroupBox Header="Port Configuration" Margin="0,0,0,10" Padding="10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Port:" FontSize="14" VerticalAlignment="Center"/>
                        <TextBox x:Name="PortTextBox"
                                 Text="5008"
                                 FontSize="14"
                                 Width="80"
                                 Margin="10,0,0,0"
                                 MaxLength="5"
                                 VerticalAlignment="Center"/>
                    </StackPanel>
                </GroupBox>

                <!-- Health Status -->
                <GroupBox Header="Health Check" Margin="0,0,0,10" Padding="10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Localhost: " FontSize="14"/>
                            <TextBlock x:Name="LocalHealthText"
                                       Text="Not tested"
                                       FontSize="14"
                                       Foreground="Gray"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="Network: " FontSize="14"/>
                            <TextBlock x:Name="NetworkHealthText"
                                       Text="Not tested"
                                       FontSize="14"
                                       Foreground="Gray"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Button x:Name="ToggleButton"
                            Content="Start Server"
                            Height="40"
                            Width="150"
                            FontSize="14"
                            FontWeight="Bold"
                            Click="ToggleButton_Click"
                            Margin="0,0,10,0"/>
                    <Button x:Name="RemoveFirewallRuleButton"
                            Content="Remove Firewall"
                            Height="40"
                            Width="130"
                            FontSize="12"
                            Click="RemoveFirewallRuleButton_Click"/>
                </StackPanel>
            </StackPanel>

            <!-- Right Column: Access URLs -->
            <GroupBox Grid.Column="1" Header="Access URLs" Margin="10,0,0,0" Padding="10">
                <RichTextBox x:Name="UrlText"
                             IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             FontSize="13"
                             VerticalScrollBarVisibility="Auto"/>
            </GroupBox>
        </Grid>

        <!-- Status Messages (Bottom, Full Width) -->
        <GroupBox Grid.Row="2" Header="Status Messages" Margin="0,15,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="MessagesText"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         Padding="8"
                         BorderThickness="0"
                         Background="Transparent"
                         FontFamily="Consolas"
                         FontSize="12"
                         VerticalScrollBarVisibility="Disabled"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>
